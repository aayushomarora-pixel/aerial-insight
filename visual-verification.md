# Visual verification

The analysis workspace was reviewed at 1280×900 and 390×844. The desktop view presents a clear two-column investigation/archive hierarchy with a visible blueprint grid, readable upload affordances, and strong contrast. The mobile view stacks the input, prompt, status tiles, and archive without horizontal overflow; controls remain reachable and readable. The signed-in sidebar collapses to a compact mobile header, while the archive remains available lower on the page.
